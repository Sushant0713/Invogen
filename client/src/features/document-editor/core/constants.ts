/** A4 canvas at ~96 DPI — shared by builder and document editor. */
export const DOCUMENT_PAGE_WIDTH = 794;
export const DOCUMENT_PAGE_HEIGHT = 1123;

export const DEFAULT_MARGINS = { top: 40, right: 40, bottom: 40, left: 40 };
